steps:
  # build the container image
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/${PROJECT_ID}/demo-cloudbuild", "."]
    # push the container image to Container Registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/${PROJECT_ID}/demo-cloudbuild"]
    # Deploy container image to Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "demo-cloudbuild",
        "--image",
        "gcr.io/${PROJECT_ID}/demo-cloudbuild",
        "--region",
        "us-central1",
        "--platform",
        "fully managed",
        "--quiet",
      ]
images:
  - gcr.io/${PROJECT_ID}/demo-cloudbuild
